# 数据库配置
DATABASE_URL=sqlite+aiosqlite:///./hre.db
# DATABASE_URL=mysql+aiomysql://user:password@localhost:3306/hre

# Dify 默认配置（可选，优先使用 UnitRuntimeConfig 中的配置）
# 如果 UnitRuntimeConfig 未配置 endpoint/api_key，则使用以下默认值
DIFY_API_URL=http://localhost:80
DIFY_API_KEY=
DIFY_MOCK_MODE=false